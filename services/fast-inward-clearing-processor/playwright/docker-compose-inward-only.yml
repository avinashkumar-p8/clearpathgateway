# This file is deprecated - use ../docker-compose.yml instead
# The Playwright tests now use the main Docker Compose configuration

version: '3.8'

services:
  # Note: This file is kept for backward compatibility
  # The actual services are now defined in ../docker-compose.yml
  # and managed by the setup-services.sh script

networks:
  default:
    name: inward-processor-network
